# TransPose 再現計画（来週）

> 目標：既存のトレーニングフレームワーク上で **Pose-S1 / Pose-S2** を再現し、**骨盤基準で全身23関節の位置**を出力する。座標系を骨盤に統一し、中間出力と最終出力の両方を損失に含める。

## 1. データと前処理（骨盤座標系への統一）

- 入力（フレーム単位）  
  - 6×IMU：加速度、姿勢（回転行列または6D）。  
  - 全身関節の3D座標（ワールド座標系）、骨盤の姿勢。

- 処理手順  
  1) 各IMUの姿勢をワールド座標から**骨盤相対**に変換。  
  2) 各IMUの加速度を**骨盤相対**に変換。  
  3) 各関節位置を**骨盤相対**に変換（骨盤位置を減算し、骨盤系に回転）。  
  4) トレーニングデータで**標準化**（平均／分散）を計算し、推論時に使用する統計を保存。  

  
## 2. モデル設計

### 2.1 共通入力エンコーディング
- 各フレームの 6×(acc+rot) をフラット化し、連結して `x^(0)` を作成。必要に応じて線形層でチャネル混合を行う。

### 2.2 Pose-S1（IMU → 末端関節位置）
- 構造：**BiLSTM** + **MLPヘッド**。  
- 入力：`x^(0)`（[B, T, D]）  
- 出力：`P_leaf_hat` [B, T, 5×3]

### 2.3 Pose-S2（[S1末端関節 + IMU] → 全身23関節位置）
- 入力：`x^(1) = concat(P_leaf_hat, x^(0))`  
- 構造：**BiLSTM** + **MLPヘッド**  
- 出力：`P_all_hat`（[B, T, 23×3]）  
- オンライン推論時：双方向を**スライドウィンドウ単方向**または小ウィンドウ双方向で近似。

## 3. 損失関数

- `L_S1 = MSE(P_leaf_hat, P_leaf_gt)`  
- `L_S2 = MSE(P_all_hat, P_all_gt)`  
- `L_total = λ1 * L_S1 + λ2 * L_S2`（デフォルト λ1=1, λ2=1）
